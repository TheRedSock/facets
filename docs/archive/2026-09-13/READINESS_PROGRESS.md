# Engine readiness implementation

Goal: implement and audit P0–P5 in `lapidary-engine-readiness-plan-2026-09-13.md`, including the user's P0 documentation archive requirement. Overall status: **implementation and validation passed; final documentation closure is recorded in ENGINE_READINESS_REPORT.md**. Baseline: `2dc22c7`. No subagents are in use.

| Phase | State | Acceptance |
|---|---|---|
| P0 cleanup | Validation passed | Final inert rig field removed; active-input/source proof, CPU/GPU checks and artifact qualification pass. Final archive index audited separately. |
| P1 shared authoring | Accepted | 52 CPU stages, durable authoring/cache GPU checks and actual Atelier invalidation/recovery passed |
| P2 cut grammar | Accepted | Declarative catalog migration, four demonstrations, 54 CPU stages and cross-backend geometry/ID gates passed |
| P3 image acceptance | In progress | Display mapping validated; final current-source motion/feature corpus and reviewed thresholds remain |
| P4 Atelier | Accepted | Saved new gem/cut/full clip workflow, generic tracks, cancellation/recovery, integrated gates and <=100 ms edit acknowledgement passed |
| P5 delivery/export | Validation passed; awaits P3 | Final optical bake, clean export/UI/error checks, all-catalog prefetch, 1080p frame budget and live texture accounting pass; appearance qualification remains P3 |

P0 evidence lives in `artifacts/readiness-review-2026-09-13/`. Seven historical Markdown documents were moved to `docs/archive/2026-09-13` with original-path and SHA-256 manifest; all moved bytes verified. Explicit scattering migration saved three affected resource roots. Before/after compiled material dictionaries match exactly across 25 recorded material occurrences. Source parsing after initial removals reported 286 scripts and zero failures. Clean unsandboxed editor import passed; sandboxed Godot cannot access its normal editor/cache directories and certificate store, so those environmental logs are not accepted as clean integrated validation.

P0 accepted implementation removes inactive fluorescence properties and the old primitive inclusion vocabulary, material scattering inheritance, deprecated performance monitor, and the duplicate synchronous clip baker. Shared sampling is `GemClipSampler`; clip GPU tests now call planner/worker. Old ladder/grain/noise, aggregate eval and speculative board-live tools/policy are replaced by planner-based `inspect_gems.gd` and the existing focused reference/lookdev tools. No compatibility aliases remain. Existing scalar/Mueller/uniaxial and convex/general numerical paths remain active.

P0 acceptance audit (2026-09-13):

- Removed-field/class/command searches over source, data, current tracked documentation and registry found no surviving references; the existing board screenshot's `board_live.png` filename is an artifact name, not a runtime backend. Buffer padding remains reserved zero (kernel v23), with unchanged numerical packing values.
- All 25 captured compiled material records match before/after exactly. Three resource roots were migrated; all source grids and refractive/absorption coefficients were preserved.
- Broad CPU run: 49 stages passed. Final changes were followed by six stable-source gates (import, 283-script parse, clips, dependency identity, planner, public authoring-surface checks). Final gate runner checks source immutability and explicit completion. Its eight result-classifier cases reject incomplete/incorrect markers, nonzero exit, exit-zero exceptions and environmental errors.
- GPU gates: production clip factory (including exposure-only master reuse), foundation, principal-index and portable asset-batch/cache/selective-delivery checks passed. No transport algorithm was changed. The full numerical/GPU/reference suite remains required at the final readiness milestone.
- Public inspector rendered 16 stones with two streams each at 64px internal/32px output/8 samples, saved batch/PNG hashes and full-frame timings, and replayed headlessly with 32 display hits and zero rendering/reprinting/restyling. This is execution/parity evidence, not image-quality acceptance; P3 owns that.
- Seven historical documents were moved and the superseded original readiness review was snapshotted under the explicitly ignored archive, each with verified SHA-256 manifest. The active docs directory now contains only index, approved scope and current progress. Current README/data/test/agent documentation points to source contracts.
- `git diff --check` passed. No default catalog rebake was requested at this intermediate phase; the existing generated delivery is historical until rebuilt at final acceptance.

Independent-reference Python is available at `artifacts/reference-env/Scripts/python.exe`. Clean Godot gates require normal cache access outside the restricted filesystem sandbox; successful approved runs produce no certificate/editor-cache errors.

P1 acceptance audit (2026-09-13):

- Added one detached authoring document with transactional edits, 64-state undo/redo, persistent comparison baseline, dirty/save/reopen, explicit replacement, external-file conflict protection and exact binary freeze. Text saving adopts reloaded values. Snapshot and source resources stay isolated.
- Shared authoring admission calls production validators; all specimen admission now compiles the actual host geometry. Capability reports distinguish scalar approximation, supported/unsupported policy combinations, reasons and unverified device requirements. Appearance review remains separate and goes stale when request, source or provenance changes.
- Physical identity excludes audited evidence/notes/labels. Material/condition values remain physical inputs. Evidence edits still revalidate and invalidate appearance review. Measured/published coefficient edits become authored derivatives retaining the source descriptor. Concentration evidence is separate from the measured spectrum; repeated edits keep bounded provenance.
- The catalog generator emits detached candidates with property diffs under generated/catalog-candidates, with source immutability checked by the suite. Removed inert GemPrint.exceptions and the surviving Atelier scattering-inheritance statements.
- Full stable-source CPU suite: 52 stages passed. The final capability status addition was followed by the 46-check authoring document gate and both GPU workflow/Atelier gates. Earlier P1 portable worker/cache and geometry/planner gates also passed. No transport algorithm was changed.
- GPU durable workflow saved/reopened a new ruby specimen and froze its request, then built through the planner/worker. Counters: 2 rendered masters, 1 print-only reprint, 1 metadata-only display hit. Actual Atelier valid-image -> immediate invalidation -> actionable shared error -> corrected recovery passed. P4 still owns replacing its synchronous execution and transient UI.
- A parser failure exposed an indefinitely open dependent test. The runner now enforces process-tree timeouts; a real incomplete Godot fixture verifies timeout failure and native child termination. Successful process execution and all eight completion/error-classifier cases pass. Failed intermediate logs were not accepted as validation.
- Source contracts: core/lapidary/authoring/CONTRACT.md; public tools/agent docs updated. git diff --check passed. P0 commit: caa9f8d. Overall readiness remains incomplete until P2–P5 pass.

P2 baseline: artifacts/readiness-review-2026-09-13/p2-geometry-baseline.bin stores the pre-replacement nominal results for both templates on eight silhouettes and all 16 catalog stones. The capture script is retained as ignored migration evidence, not a production compatibility implementation.

P2 acceptance audit (2026-09-13):

- Replaced crown-row/table/pavilion-specific compilation with one declarative facet program. Named groups own independent index wheels, angle and scalar offset/anchor constraints, and explicit earlier-surface meets. Horizontal terminations are optional groups and participate in manufacture. Authored girdles remain on GemShape; convex custom CCW unit-radius polygons are admitted. Shape sector coupling and cut_row schema are deleted. Unused fixed nominal-cut condition-variation targets were removed; explicit cut overrides and workmanship variation remain.
- Both old templates migrated; triangle index sets are explicit brilliant_triangle and step_triangle resources. The old compiler's sequential star dependency was discovered by numerical comparison and encoded as ordinary named data groups. No special star branch or compatibility grammar remains. Unknown/unused parameters, bad references, wrong-facing or nonfinite meets, unsupported syntax, unbounded/empty geometry and excessive normalized height fail admission. Later clipping/manufacture can erase an intermediate construction face; diagnostics name it without changing the already defined meet.
- Stable int32 IDs are checked hashes of group/index names. Manufacture is independently seeded per facet. Renaming a cut label or adding a subsequently pruned facet leaves existing manufactured geometry/IDs unchanged. Facet wire layout is unchanged; kernel contract documents that IDs are not array positions.
- Four saved specimens under data/lapidary/cut_examples demonstrate pointed crown/flat bottom, independent pavilion indices, mixed rows and custom girdle. Their program hulls convert to closed oriented meshes. GemCutInspection and inspect_cut.gd produce hull-derived facets, meet contacts, dimensions and sections. SVG geometry was visually inspected; its text/hover labels require an SVG viewer because Godot's rasterizer omits text. P4 owns integration of this data into the new Atelier panels.
- Nominal baseline: all 16 old template/shape combinations retain facet counts. Maximum nearest corresponding plane coefficient error is 1.1920929e-7. Captured catalog geometry and before/after renders are retained under artifacts/readiness-review-2026-09-13.
- Render preservation: all 16 catalog stones, two streams, 224px internal/112px output, 128 samples. Maximum opaque RGB change is 1 LSB; maximum opaque RMS is 0.02913 LSB. Alpha differs by at most 1 LSB. Black/white linear-light composites differ by at most 1 encoded LSB, RMS at most 0.01495 LSB. Paired-stream noise in the baseline is 0.305–0.959 LSB. This validates P2 preservation, not P3 image-quality acceptance.
- Full CPU suite: 54 stages passed. Final example mesh checks were followed by their dedicated gate and source parse. GPU foundation, principal-index, polarization and crystal numerical gates passed. The four new designs also completed all three transport policies with an isotropic control, including a manufactured pointed specimen; primary geometry records were byte-identical across backends and every visible GPU facet ID belonged to the compiled stable IDs. Beauty equality was not asserted between scalar and polarization-aware models.
- Portable asset-batch/cache and actual Atelier invalidation/recovery passed. Generalized optimize_cut now accepts explicit --vary parameter axes; the registered two-candidate diamond workload completed baseline, independent-seed confirmation and held-out evaluation (about 73 seconds), with no automatic catalog promotion.
- Evidence snapshots: p2-cpu-validation (54-stage run), p2-final-validation (final GPU/source/example gates), p2-render-comparison.json, p2-compare.log, p2-render-before/after, and the referenced facet-program-gpu report. P1 commit: 5d79894. Overall readiness is still incomplete; P3–P5 remain.

P3 work in progress (not accepted):

- Print version 4 keeps signed RGB through Oklab gamut mapping. Public encoded views are HOUSE_PRINT and DISPLAY_PREVIEW; linear XYZ remains data. Live/offline mastering is one GPU implementation. Print dispatch now has print-only source identity; view-only factory changes reuse the optical master. Structural schema/host edits conservatively invalidate older pre-refactor caches.
- Spectral gate: all 401 wavelengths, neutral exposure ramp, three coverages and zero alpha. Live/compressed-offline outputs are byte-identical. Independent color checks measured maximum tested hue error 0.82 degrees (house with styling disabled for this invariant) and 0.94 degrees (preview); maximum Oklab L error 0.00164. The former negative-RGB preclip exceeds 43 degrees on the same inputs. This is a bounded engineering display check, not universal perceptual calibration.
- Seven explicit saved acceptance specimens are admitted. The detailed-fracture pilot is preserved under p3-fracture-pilot; the shipping-size boundary test uses an explicit six-micrometer planar cavity to isolate boundary preservation without adopting the unaccepted fracture morphology as an appearance preset.
- Corpus tool freezes jobs, verifies content keys, records independent sample streams and total production-worker wall costs, and resumes completed images/checkpoints. Four distinct poses form a closed half-second tilt sequence. Full planned coverage is 112/256/512 px, two rigs, two production candidate profiles and independent unfiltered references. A second 2048-sample reference at the fixed initial pose measures reference uncertainty.
- Analysis measures linear-light black/white/game composites, spatial RMS, mean bias, residual motion error, real-motion gain and loop-boundary residual. Internal feature masks derive from feature-on versus matched feature-off reference renders, never primary-surface geometry correspondence. Synthetic fault cases reject frozen frames, blur and removed features. Threshold selection and complete corpus inspection remain outstanding.
- Preliminary stationary pilot values are not phase acceptance. The stable-source 54-stage CPU suite passed. The complete corpus is rendering; partial results are not acceptance. P4 proceeds in the scene/tool layer while optical sources stay frozen; P5 remains pending. A UI interaction-budget preference was requested asynchronously while P3 proceeds.


P4 work in progress (not accepted):

- Replaced the synchronous Atelier execution with a scene-thread client and one child process owning the production frame worker. Requests are detached binary bundles with checksums and monotonic generations. A discovered cancel/request same-generation race is fixed and covered by a deterministic protocol test.
- Resource inspector edits nullable branches, typed arrays, scalar values and named enums through the durable authoring document. Actual widget tests cover material units, condition arrays, save/reopen, source isolation and undo. Headless authoring neither starts the worker nor writes coordination files. Invalid edits clear images immediately.
- Preview and build use production planner/jobs/worker; a newly named specimen and declarative cut save/reopen/build without engine changes. The same frame identity resolves in the packed library. Source parsing, actual UI controls and windowed workflow checks pass.
- Cancellation after partial sampling retains estimator checkpoints, publishes no completed display, rejects stale responses and resumes successfully. Close during active sampling also passes clean ownership release. The worker uses bounded sample units; scheduling efficiency and uncontended latency still need measurement and optimization.
- Compiler-derived top/front facet and section overlays were visually inspected, with millimeter dimensions and explicit pre-rounding/cleavage labeling. Built preview and native-size comparison are visible. Device reports distinguish allocator ownership, limits and wall time from GPU timestamp availability.
- Full acceptance corpus remains active on the GPU, so concurrent UI frame timings are contention evidence only. Generic orientation tracks, final interactive-budget acceptance, integrated phase validation and P5 delivery/export remain outstanding.


Checkpoint ed9bf66 records P3 mastering/corpus and P4 authoring implementation without claiming phase acceptance. The 55-stage CPU run found one obsolete test helper call; it was migrated to the document API and its targeted gate plus source parsing passed. All other stages passed that run. A stale PowerShell board_live rung option was also removed.

P5 work in progress (not accepted):

- Runtime GemAssetLibrary moved to core/delivery. Dedicated page-wire identity removes dependencies on the optical authoring/cache graph while preserving exact v1 page keys; GPU compression/roundtrip gates pass. Live weak texture references measure resident bytes outside LRU ownership and drop after consumers release them.
- One explicit GemDeliveryCatalog maps logical tile IDs to asset IDs and rest/upgrade roles. Build/runtime admission rejects missing roles/clips, duplicate identities and invalid rest/upgrade loop behavior. The default data maps all 16 tiles explicitly. TileView has completion/interruption/restart/return-to-rest behavior; the old ignored rotation API, tier palette and unused debug_color fields are removed.
- Run startup prepares the chosen ladder plus reachable upgrade targets before showing the board. Missing delivery fails with a loading error. Deterministic synthetic test assets exercise actual playback with logical IDs different from physical IDs; no generated-cache-dependent test skips or production tint remain in that gate.
- Import, source parse, clip CPU, actual board-consumer fixture and GPU library checks pass. A native Node.ready name collision was fixed; the check runner now stops dependent stages after import/source failure. Failed runs are not acceptance evidence.
- Official Godot 4.6.1 export templates were downloaded and SHA-256 verified into artifacts/toolchain/godot-4.6.1. Only the matching Windows debug/release executables were extracted. Export preset/package audit, cold-burst performance, corrupt/missing-pack/reload checks and final default rebake remain outstanding. Optical corpus source/inputs remain frozen.

P5 delivery/export checkpoint (still not phase acceptance):

- Removed inert DebugFlags autoload/outline controls. Runtime source staging now excludes authoring code and generates its own class cache; the initial source-tree export carried 120 excluded class entries despite omitting their scripts. The package inventory gate caught and rejected that output.
- Added pinned template provisioning, explicit runtime export preset, fresh staging build, full page/completeness audit and external package playback probe. Positive completion parsing now strips ANSI styling; 10 result-classification cases and bounded-process cleanup pass. A sandboxed process-test startup crashed; the same check with normal Godot runtime permissions passed. That environmental attempt is not accepted evidence.
- Corrected the probe's working directory: an earlier run proved playback but could still see the repository through filesystem fallback. The final isolated build/probe in artifacts/package-build/20260913-154344-2995 passes with source_tree_available=false. The package contains exactly Facets.exe, Facets.pck and gem-assets.pck, in a path containing spaces and ø. Automated probes use the matching editor executable against the exact PCK because standard export templates disable script overrides.
- Actual release UI was separately inspected through authorized Computer Use: Play -> Menu -> Play shows all 64 delivered fixture tiles; absent and corrupt packs keep the board hidden and name the failing path; Menu remains usable after missing assets. All test windows were closed. Evidence: artifacts/export-audit/release-ui-audit.json and isolated-*-ui.log. These use explicit synthetic assets, not final optical content. Concurrent corpus rendering makes frame timings unsuitable for performance acceptance.
- Prefetch now reuses live pages even after LRU eviction. Tests with zero cache ownership reject an over-budget set before upload, retain/reuse prefetched textures without cold animation loads, count old-library texture references across replacement and drop them after release. A separate 128 MiB desktop prefetch cap is explicit; final-content budget acceptance remains outstanding.
- The appearance process ended without completion or an error message. Its cause is unknown. Verified no corpus workers remained, inspected the abandoned claim snapshot, then recovered only coordination tokens using recover_gem_store.gd. Completed images and estimator checkpoints remain intact. The resumed process uses hash-verified cached results; optical sources remain frozen.


- Stable-source broad validation passed all 55 registered CPU stages. Evidence snapshot: artifacts/readiness-review-2026-09-13/p5-cpu-55/results.json. Subsequent targeted gates cover the final delivery changes. Added test_run_delivery plus simulation smoke and RNG to the runner, bringing its CPU inventory to 58. Simulation smoke reports 113 assertions passed; the RNG command now returns failure on same-run mismatch and a positive marker (cross-platform parity still requires another platform).
- A real RunScene regression reproduced a post-await startup bug: a missing tile failed during deferred board rebuild, then the startup coroutine unhides/unlocks it. The final readiness guard rechecks generation, tree membership and delivery error after the awaited frame. The original regression fails, the fixed case passes, and an explicit valid board recovers without changing simulation state. Before/after logs are retained under p5-delivery-final.
- Runtime manifests now reject orphaned frames/pages as well as dangling references, dimensions and corruption. The page loader validates before reusing a resident reference; dead weak entries are pruned. Import/source parse, factory CPU, board consumer, real RunScene delivery and GPU compression/PCK playback all pass after these changes.

- Portable factory GPU gate passes after the delivery extraction. The final source checkpoint also builds and passes isolated PCK inventory and 64-view playback in artifacts/package-build/20260913-160134-5047, with source_tree_available=false. The earlier separate release UI navigation/error checks remain functional evidence; final optical-content rebake, uncontended performance/working-set acceptance and integrated P3/P4/P5 acceptance are still required.
Checkpoint f7f0209 records P5 delivery/export implementation. P3/P4/P5 acceptance and the overall goal remain incomplete.

- Added native-size chronological reference/draft/detail review sheets to check_appearance.py --inspect --gallery. Inspected clear/strongly absorbing 112px game-background motion frames, rounded 112px white-background frames and the clear daylight large comparison. No obvious lost facets or blurred motion signal in those reviewed subsets; this does not accept missing rough/internal-feature cases. The partial analyzer still reports incomplete and no accepted thresholds. The full GPU corpus stage now runs metric fault tests and strict image analysis afterward, so rendered-frame completion alone cannot pass P3.
- Captured all 304 current catalog clip frame jobs before the generic orientation-track migration. Binary requests, exact job hashes, poses, camera offsets, exposure/lighting samples and timing metadata are in artifacts/readiness-review-2026-09-13/p4-clip-baseline. The headless capture completed without rendering or changing catalog data.

- User selected desktop acceptance budget: delivered gameplay p95 frame time <=16.7 ms (60 fps target), Atelier edit acknowledgement <=100 ms. Preview completion latency is measured separately. Contended measurements do not establish acceptance.
- Session collection now retains only two newest preview PNGs and removes older consumed request bundles; current/future requests, logs, completed delivery and resumable optical store remain intact. Client retries superseded image reads using a hashed byte buffer. Import/source/actual Atelier UI protocol checks pass. The obsolete kernel-v1 TRACER_NEEDS.md was moved into the checksummed docs archive.

- Stable-source integrated CPU validation passed all 58 stages after session collection and image-read retry changes. Snapshot: artifacts/readiness-review-2026-09-13/p4-session-cpu-58. Appearance metric fault tests also pass. Partial review now includes all rough cases: unfiltered reference noise reaches 2.51 LSB at large size, so rough reference quality itself needs improvement before setting limits. Localized-volume daylight112 has no pixels above the current feature-detection threshold; this fixture requires a stronger explicitly authored diagnostic signal, not a silently weakened pass criterion.

- Independent Python references now require explicit completion markers and are included in the check runner's result ledger. Ten references passed directly. The finish-field reference correctly rejected stale gem_common.glsl results; refreshing finish_fields_gpu_check produced 256 passing comparisons (max matrix error 4.68e-7, width relative error 3.15e-6). Original rejection and fresh evidence are retained in reference-completion.
- Added separate Atelier edit/preview latency and full-catalog package burst/rest performance gates. They parse successfully but have not passed uncontended performance acceptance. The external release capture uses pinned PresentMon2.5.1 (SHA2569bec3083069f58f911e6a512f4806db51a27bd096103087bc1d05ef54c80a191). Its short ETW access diagnostic returned access denied: final native release measurement requires a Windows elevated or Performance Log Users token. No group membership or system service was changed.

- Generic clip schema now replaces all still/turntable mode fields and branches. GemOrientationKey arrays plus monotone orientation time curves cover stills, holds, arbitrary axes, full turns and tilt/return. Loop geometry/light/envelope endpoints are admitted explicitly; continuous Bezier control bounds reject negative/overshooting time curves. Empty orientation tracks are invalid, so old mode-only resources are not accepted. No compatibility sampler remains.
- Migrated three catalog clips and every tool/test caller; added a 16-frame tilt_return data example. All 304 baseline frame-job digests are exactly identical after migration, with zero quaternion/camera error and matching lighting/exposure/playback timing. An initial evidence-script parse error was fixed, then exact comparison failed only on JSON-round-tripped fractional timestamps; a 1e-12 timestamp tolerance yields zero failures. Optical job hashes themselves remain exact.
- Actual Atelier controls now author orientation keys in Euler degrees and cubic curve values/slopes; reusable clips and curves save through public admission. Headless UI tests pass. The windowed workflow saves a new gem with changed table geometry and a full tilt sequence, previews/builds every expected frame, rejects stale output, cancels/resumes partial work and closes cleanly. Portable asset-batch checks pass. Targeted source/sampler/planner/document/dependency gates also pass; evidence is under p4-generic-clips.
- Planning update: remaining clip schemas necessarily change full source identities. The still-running original corpus remains pre-migration exploratory evidence. Frozen source inventory: p3-pre-clip-migration-source.json (optical source checkpoint298c7d1). Preserve/archive that corpus when its process ends, then generate final appearance acceptance with current identities. Do not relabel old pixels as current-source acceptance. No GPU shader/host transport code has been changed while that process is active.


- User accepted desktop budgets: gameplay p95 frame time <=16.7 ms (60 fps), Atelier edit acknowledgement <=100 ms. Preview completion is measured separately. Desktop testing may resume as needed without another permission request.
- Current 58-stage CPU run passed after generic clips, schema-2 corpus configuration and unused chromophore UI-color removal. Snapshot: current-cpu-58/results.json. Exact 16-specimen physical-input and compiled-material digests match before/after metadata removal. Removed stale fluorescence-override/wear-model wording; no optical coefficients changed.
- Schema-2 corpus assigns explicit candidate/reference profiles per case. The rough stationary pilot at512 px uses a16384-sample unfiltered reference and a1024-sample/one-pass candidate. Reference noise <=0.941 LSB; candidate RMS <=1.814 LSB, mean bias <=0.074 LSB over black/white/game composites. Native reference/candidate inspection preserves facet boundaries and broad reflections. Full motion/rig/size acceptance remains required. Evidence: artifacts/appearance-rough-pilot/comparison.json.
- Strengthened the localized-volume diagnostic with explicitly authored absorption. The daylight112 pilot has104–118 detected feature pixels, gain0.999–1.001, correlation>=0.9994 and relative residual<=0.034. This is a diagnostic fixture, not natural-cloud calibration or final corpus acceptance. Evidence: artifacts/appearance-volume-pilot/visibility.json.
- Stopped verified obsolete corpus PID4032 and preserved its complete directory as artifacts/appearance-corpus-pre-migration, including interrupted checkpoints/tokens and an explicit historical-incomplete archive note. It must not be resumed or relabeled as current acceptance. This avoids spending more GPU time on superseded authoring identities. Both old corpus and rough pilot processes are now closed. Removed the two unused fluorescence flag constants and aligned new GemPrint defaults with house print version4 before final source freezing.
- The expanded isolated-package probe passed functionally:64 loaded views, all16 declared bindings,51 repeated fixture-animation bursts, natural return to rest and no additional page loads. source_tree_available=false. Evidence: artifacts/export-audit/catalog-probe-functional.json. Its concurrent-render frame timings (~225 ms p95) are explicitly not performance acceptance.
- Empty corpus case/profile selections now fail instead of printing success after zero jobs. Targeted empty-selection regression and appearance metric fault checks pass. Final current-source corpus, integrated GPU/reference checks, actual optical rebake and native-release performance/UI acceptance remain outstanding.

- P4 measured-response gate passed after repairing a transient JSON mailbox read race. Readers now retain the last complete request during a publication gap; incomplete responses are retried without being consumed. A deterministic truncated/empty/non-object-message regression, actual save/new-cut/full-clip/cancel/resume/build/close workflow, source checks and uncontended latency all pass (5 stages). Before-failure and after-pass logs: p4-latency-race.
- Uncontended224px internal/112px output64-sample workload: five edits each, acknowledgement p95 material51.82 ms, geometry49.13 ms, pose46.46 ms, print49.22 ms. All physical edits render a distinct optical master; print edits reuse one. Completion p95:2.27/2.32/2.20/0.284 s. Single cold startup: first image1.62 s, complete3.52 s. UI frame p95 while rendering24.94 ms is separate from the delivered-gameplay16.7 ms target. No preview-completion promise is made for arbitrarily complex specimens.
- Final optical-source freeze follows these fixes. Run the integrated CPU/GPU/independent-reference gates before final corpus rendering; appearance acceptance remains pending reviewed full coverage, and P5 remains pending real optical-content packaging and performance acceptance.
- Checkpoint ac58ff7 freezes the optical sources after the pilot/cleanup/mailbox fixes. Integrated registered checks are running in exec session3266 (all enabled CPU/GPU/reference-mode stages except appearance_corpus and the already-passed atelier_latency_check). Because -Only suppresses the runner's additional Python-oracle block, run the11 Python references separately after their GPU exports finish; do not claim them from this invocation alone.
- Remaining bounded CLI fix reproduced before any edit: prepare_gem_jobs.gd --stone=quartz --clip=tilt_return fails because its convenience branch filters only idle/turn/flash. Saved batches and Atelier already support arbitrary admitted GemClip resources. After the checked-source lock releases, allow explicit valid catalog clip IDs to load directly while keeping the default catalog clip set unchanged. Evidence: clip-selector-before.log. This tool-only correction will not alter frozen optical source identities.
- Added current docs/AUTHORING_WORKFLOW.md linked from docs/README.md. Historical resolved-inclusion motion analysis shows preserved causal features at all sizes/rigs (gain~1, relative residual<=.05) but remains explicitly pre-migration exploratory evidence: historical-internal-feature-review.json.
- Read-only legacy-call audit found one superseded verification tool: tools/atelier_screenshot.gd (and UID) still calls removed debug_status/debug_set_volume APIs and has no active caller or registered gate. Remove it after the source lock releases; atelier_admission_check already captures current screenshots and tests the actual worker/document path. Do not add adapters or preserve a duplicate harness.


P4 acceptance audit (checkpoint ac58ff7 plus tool-only cleanup de3bc75):

- One detached request/planner/worker path serves preview and builds. Actual scene tests create/save/reopen a new specimen and cut, inspect manufactured facets, build all16 tilt frames and match their delivery identities to the saved request. UI edits, external-resource isolation, undo/redo, dirty/save/replace and derivative-evidence gates pass.
- Generic orientation keys/time curves and rig/power/exposure envelopes share the production sampler.304 catalog frame-job identities were preserved by migration. Current windowed admission checks exercise the full authored16-frame tilt, stale results, cancellation after partial samples, checkpoint resumption and shutdown during active rendering.
- The agreed100 ms acknowledgement budget passes all20 distinct material/geometry/pose/print edits at the documented224/112px64-sample workload. Preview completion and one cold startup are reported separately, without a universal-complexity timing claim. Scene/worker allocator and device limits are reported. No synchronous optical path or old screenshot/debug-API harness remains.
- Final integrated validation:106 Godot stages plus color analysis passed (107 records), snapshot final-integrated-ac58ff7. All11 independent Python references passed against freshly produced exports, snapshot final-references. Together118 integrated/reference records pass; the separately measured latency gate also passed. Optional cross-platform deployment and arbitrarily complex-content timing are not inferred from these Windows results.
- Tool-only cleanup de3bc75 admits any valid catalog clip file stem through the public CLI, rejects unknown/malformed/duplicate arguments and invalid boolean values, and removes obsolete atelier_screenshot.gd plus UID. Six negative CLI cases reject correctly; tilt_return plans16 requested frames. The unchanged default is19 requested frames (idle1, turn12, flash6), with14 unique displays for quartz. An initial verification-script expectation of14 *requested* frames was corrected after inspecting the authored timings; implementation output was already correct. Import/source checks pass after cleanup.
- Current corpus planning verifies exactly930 immutable jobs and current source/configuration hashes: artifacts/appearance-final-plan/verification.json. Final render is LIVE in exec session73706, log artifacts/appearance-final-render.log, output artifacts/appearance-corpus. All prior integrated/render/reference processes are closed. Keep optical sources and corpus inputs frozen until this render completes. P3 thresholds and full image review, then P5 final optical rebake/package/native UI/performance acceptance remain required; overall goal stays active.
- Added synchronized native-size motion review in checkpoint85ed262. check_appearance.py --inspect --gallery now writes review/index.html alongside the composited sheets. Browser inspection verifies112px/draft and256px/detail comparisons, white/game backgrounds, pause/step/slowed playback and an empty browser error log. This is viewer validation and partial clear-case inspection, not final corpus acceptance. Evidence: appearance-viewer-validation.json. Metrics fault checks and generated JavaScript syntax validation pass.
- Review server is local-only127.0.0.1:8780, PID18140; serves artifacts/appearance-corpus/review. CUA reviewTab is tab1 in browser1, paused and marked for handoff. Regenerate the gallery and reload that tab as cases complete. Current tool-only viewer/CLI commits do not participate in optical source identity, and the930-job source/config verification remains valid. Final render session73706 remains live; keep all optical sources and acceptance inputs frozen.

- Clear-faceted final-source case review completed: all18 native chronological sheets cover112/256/512px, two rigs and three backgrounds, four poses and reference/draft/detail. Draft preserves facet edges, moving bright wedges, small chromatic fringes and silhouette. Independent-stream maxima: spatial0.9213, bias0.07205, temporal1.2059, loop-seam1.1789, reference noise0.1715 LSB; motion gain0.99891..1.00019. Case-specific proposed regression limits and126record hashes are retained in appearance-decisions/clear_faceted.json. This accepts only the reviewed diagnostic comparison; full P3 acceptance remains pending the other cases and strict final thresholds.
- Final CLI audit found build_gem_assets.ps1 still accepted exit-zero stages without positive completion. It now uses the shared result classifier; prepare/render/pack/collect emit exact markers only after success. Sample-limited partial workers preserve checkpoints without emitting completed-render evidence. An incomplete exit-zero fixture is rejected; actual catalog planning and isolated cached worker/pack/collection stages pass, along with import/source checks. Evidence: build-completion. The first ad hoc regression command had a PowerShell here-string quoting failure; the corrected saved script passes. These tool-only changes do not alter frozen optical identities or corpus inputs.
- Checkpoint f918cd3 records the asset-stage completion fix. Current-source optical rendering remains live in session73706; latest observed215/930 records. Gallery analysis has40 complete comparisons, with no source/configuration errors and expected incomplete/missing-threshold status. Strong-absorption partial review covers all six112px sheets and the three gameplay256px sheets; remaining large/daylight sheets require review. Exact inspected-sheet hashes and coverage are in appearance-decisions/strong_absorption-partial.json. Review tab1 remains paused at strong_absorption/gameplay112 frame2 and marked for handoff. No P3/P5 or overall completion is claimed.
- Final wrapper option audit reproduced silent defaults for -Samples -1 and unused -SpecimenSeed17 without a recipe (both previously exited0). Negative Samples/Resolution now fail parameter admission; an explicitly supplied SpecimenSeed requires Recipe. Three invalid-input cases reject; explicit zero resolution/samples still select rung defaults; a valid recipe/reference preset/seed17 plans successfully. Evidence remains in build-completion. No optical source or data changes were made.
- Documentation closure check verifies all9 archived records against their SHA256, the archive ignore rule, the four current root documents and all15 local Markdown links. Evidence: docs-current-audit.json. Strong-absorption native review now includes all gameplay512 backgrounds as well as both-rig112 and gameplay256 views (12sheets); the remaining daylight256/512 review follows the completed render.
- Strong-absorption final-source visual review is now complete across all18 native sheets: two rigs,112/256/512px, three backgrounds and four poses. Draft retains saturated planes, dark fine wedges and changing reflected highlights; measured maxima are spatial0.6830, bias0.02043, temporal0.8522, loop-seam0.8657, reference noise0.1520 LSB; motion gain0.99957..1.00053. Selected case regression limits and126record hashes are in appearance-decisions/strong_absorption.json; the partial review is explicitly superseded. Clear and strong cases are reviewed; remaining five cases and final strict thresholds still block P3 acceptance. Tool-only wrapper admission fix is committed as af3488d. No optical input changed. Review tab1 is refreshed and paused, marked for handoff.
- Rounded final-source112px gameplay motion is complete and reviewed across three backgrounds (four poses, reference/draft/detail). Draft retains the moving facet highlights and outline, but has higher numerical residual: spatial1.4467, temporal2.0330, bias0.09322 LSB; detail spatial0.6996, temporal0.9863, bias0.01991. Reference noise0.21496 LSB. Profile selection remains pending larger sizes and daylight views. Partial evidence: appearance-decisions/rounded-partial.json. Analyzer has52 complete comparisons and only the expected incomplete/no-threshold status. Live renderer session73706 has moved on to rounded256px. Recent waits were verified against this live handle, with new completed frames; no restart was performed.
- P5 final default catalog is frozen independently under artifacts/final-catalog-plan (planning only). Exact source/job hashes,224 display identities and176 master identities validate; all16 tile definitions resolve all32 declared rest/upgrade roles with correct loop/completion metadata. Default304 requested frames include authored idle/turn/flash. Final-catalog-plan-verification.json retains current manifest/catalog hashes and each specimen's exact128-sample policy,224/112 dimensions and physical-feature summary. All default surfaces are smooth, with no rounding/cleavage/explicit defects/localized volume fields; authored homogeneous scatter ranges0.0005..0.074191191/mm and banding contrast0..0.17. This is plan/binding coverage, not rendered-quality or runtime acceptance. The temporary verification script initially used job.policy instead of job.quality; identified audit processes5772/19332 were stopped, the typo corrected, and a bounded rerun passed. The separate source-file lookup after successful preparation used an incorrect filename and was corrected by file inventory; it did not invalidate preparation. The active corpus process was left untouched and continues rounded256px references.
- Started the full default production bake through tools/build_gem_assets.ps1 in exec session49695 while corpus session73706 continues. Both stores are separate. The prepared production manifest has the same source inventory, clips, display/master identities and geometry as the independently audited plan. Freshly saved .res files differ byte-for-byte, so the first byte-equality assertion failed; a bounded decoded comparison then proved exact GemContentIdentity equality (including provenance/schema code) for all224 jobs and separately verified every binary against its own manifest checksum. Evidence: final-catalog-build-identity.json/log. No claim of reproducible bytes across independent saves is made. Build rendering was last observed at50 new masters and6 print-only outputs. Performance acceptance must wait until both GPU workloads finish; corpus/bake wall costs are explicitly contended. P3/P5 remain incomplete.

- Full default production bake completed successfully: all four prepare/render/pack/collect markers pass,176 optical masters plus48 reprints produce224 displays,304 requested frames and48 clips.16 packed pages total6,053,312 GPU bytes and1,218,122 encoded page bytes. Build logs are preserved in final-default-build. Fresh real-asset package artifacts/export-audit/Final optical ø package passes isolated inventory and playback (source_tree_available=false,64 views,all16 bindings,38 animation bursts,no additional page loads after prefetch). Actual release Computer Use verifies Play, invalid swap restoration, valid merge20->19 moves, Menu, second Play, missing/corrupt pack errors and hidden boards; all test processes closed. Exact package hashes/evidence: artifacts/export-audit/final-optical-release-ui-audit.json and artifacts/package-build/20260913-190403-5255. Concurrent corpus timings are not acceptance. P3 full appearance review and P5 uncontended performance remain incomplete; overall goal active.

- Final delivery visual audit inspected all304 requested frames at native112px in four chronological contact sheets (16 gems, turn/idle/flash). No missing frames, visible atlas bleed or clipped silhouettes; turns/flash return to idle. Evidence: final-delivery-review/report.json. This is delivery inspection, not a claim of final game art direction or diagnostic-reference acceptance. Rounded corpus review now covers all gameplay sizes/backgrounds plus daylight112 (12 native sheets); fine moving facets/lines and lower daylight highlights remain matched. Larger daylight views are still rendering. Current partial analysis has64 comparisons and the expected missing-threshold/incomplete status. No optical sources were changed.

- Paused corpus deliberately for isolated performance validation, after reading a valid816/2048-sample checkpoint for rounded daylight256 frame1 reference. Exact renderer PID20048 and console24296 stopped; session73706 is terminal(exit1 due intentional stop). Verified no other Godot worker, then inspected and recovered snapshot1c4b2379845caeb93f43316c757fd232f561eb3ca910d9241e6a7d9c28a5dde6. Recovery archived only two coordination entries; completed346 records and checkpoint artifacts remain. Evidence: corpus-performance-pause. This is an intentional checkpoint/resume transition, not an unexplained renderer failure.
- Initial uncontended package timing failed the agreed16.7ms p95: burst17.012ms/rest17.057ms. User-authorized elevated PresentMon actual-release capture confirmed16.9662ms p95 over1797 intervals, while GPU time p95 was6.7951ms. Added explicit mailbox V-Sync and120fps application cap (two project settings), keeping tear-free presentation and bounded application work. No optical source/configuration changed. An isolated1920x1080 experiment passed; the fresh final package passes inventory, all16-bindings/64-view playback, and frame budget at both1600x900 and1920x1080.
- Final1920x1080 all-catalog probe: cold board ready228.004ms, remaining catalog prefetch127.927ms, burst p95=16.296ms, rest p95=15.724ms; all16 texture pages total6,053,312 resident bytes, including references outside cache ownership, and no animation cold loads. Actual final release at1920x1080:3595 present intervals over30seconds, p95=16.5176ms, p99=16.6837ms, max19.8881ms; four-gem swap/cascades included. P95 target passes; no max-frame guarantee is claimed. Exact capture/package/workload evidence: artifacts/release-frames/final-paced/report.json, artifacts/export-audit/final-paced-1080p.json, final-paced-ui-audit.json; package artifacts/export-audit/Final paced ø package. Windows UAC was approved by the user; bounded capture helpers exited, no persistent system setting changed.
- Revalidated Atelier after presentation change: import/source and latency gates pass, all20 edit acknowledgements <=39.649ms (material33.221,geometry39.649,pose36.425,print34.295 p95). Completion p95 remains separately2.364/2.463/2.361/.282seconds; cold first image1.782seconds. Snapshot: mailbox-validation. Only project.godot differs between the previous and final package runtime sources; executable and optical pack hashes are identical, so prior missing/corrupt-pack logic evidence remains applicable. P5 delivery/performance criteria now pass; formal phase closure waits for P3 appearance qualification. Overall goal remains active.

- Checkpoint da79035 records the bounded presentation fix. Corpus resumed in exec session5583, log artifacts/appearance-final-resume-performance.log, using the existing store and346 completed images. Pre-resume verification confirms the interrupted reference checkpoint retains1008/2048 samples. The resumed reference's observed wall time covers only its resumed attempt; prior interrupted cost is not a complete fresh-render timing (the analyzer already labels resume/concurrency limitations). Do not use this reference attempt to claim a fresh total cost. No production candidate-profile frame was interrupted.

- Closure evidence indexing now binds five inspected requirement groups to exact test sources, positive result records and log/report hashes: durable authoring/identity, required cut examples/manufacture, actual Atelier worker workflow, delivery/simulation boundary, and final real-asset export/performance. Evidence: closure-evidence-index.json. It is explicitly a partial index, not final phase closure; P0 removal/migration/archive, P2 migration/backend equivalence, full P3 and final documentation still require the final requirement-by-requirement audit. Source tests inspected remain unchanged since the integrated ac58ff7 run; later presentation changes have separate current latency/export/ETW evidence.

- Closure audit now verifies seven evidence groups, adding P0 material/archive/removal and P2 migration/backend coverage. All25 pre/post compiled material digests match; nine archive checksums and four current root documents verify; deleted public schema/baker symbols are absent from active source/data. All16 migrated cut/outline plane sets retain counts with maximum difference1.1921e-7 (gate2e-6); all16 catalog render differences lie below measured paired-stream noise. Fresh runner tests pass all10 result-classification cases plus actual timeout/child-termination/success checks. Historical material inventory logs contain a certificate-store environment error and are used only for exact inventory comparison, not as clean validation runs; current integrated gates pass separately.
- Rounded final-source review is complete: all18 native chronological sheets, two rigs, three sizes/backgrounds and all four poses; browser inspection additionally steps all four detail/gameplay112 poses. The12 previously reviewed sheets retain identical hashes. Selected detail: maximum spatial0.820626,bias0.029740,temporal1.149936,loop-seam1.077504,reference-noise0.238443 LSB; motion gain0.999723..1.000089. Case-specific limits1.0/.05/1.4/1.4/.3 and all126 record hashes are saved in appearance-decisions/rounded.json. Detail approximately halves draft residual without erasing fine highlights; its extra cost remains explicit. Current analyzer has72 complete comparisons and expected incomplete/missing-threshold status. Renderer session5583 is live on rough surfaces. P3 and overall goal remain incomplete.
- Rough gameplay112 review covers three native background sheets and all four poses. Draft visibly softens some highlight shapes; unfiltered detail retains grain. Refined is provisionally closer to the reference (spatial1.7902,bias0.10158,temporal2.4373 LSB versus draft4.3227/.68248/5.5257; reference noise0.54212). No final profile/limits until larger sizes and daylight pass. Evidence: appearance-decisions/rough-partial.json. Latest analyzer has78 complete comparisons; session5583 remains live on rough gameplay256, first reference completed133664.755ms. Closure index now has eight inspected groups, including current result/process-runner evidence. Overall acceptance remains pending full P3 and final closure.
- Closure index expanded to eleven inspected groups. Generator ownership audit verifies all49 current source/candidate SHA pairs from the integrated generation diff; the implementation writes only fresh generated candidates and the integrated runner covers source/data immutability. Actual Atelier control tests cover scalar/unit edits, nullable resources, typed collections, named curves, new cut/clip saves, undo, source isolation and mailbox/session cleanup. Print gates cover401 spectral hues in both views, negative-RGB regression sensitivity, neutral ramp, alpha, offline/live byte parity, unchanged XYZ and reprint-only view edits. No new implementation gap found in these checks. Session5583 remains live; rough gameplay256 pose1 reference completed143873.007ms and pose2 candidates completed before its reference. No optical sources/configuration were changed; overall goal stays active.
- Rough gameplay review now covers all nine native112/256/512 sheets and three backgrounds, four poses and all reference/draft/detail/refined columns. Larger views show draft flattening some reflection gradients and softening/chopping highlight detail; unfiltered detail has conspicuous grain. Refined preserves central gradients, edge transitions and moving bright wedges more closely, with modest residual grain at512. Maxima across gameplay sizes: refined spatial2.31528,bias0.10668,temporal3.14364 LSB; reference noise0.93989. Partial review hashes/comparisons updated in rough-partial.json; no final profile acceptance. Analyzer session63595 completed with90 comparisons and expected incomplete/missing-threshold status. Live corpus session5583 subsequently completed daylight112 (not yet reviewed) and is rendering daylight256 frame0 reference. Keep sources frozen and complete daylight review before choosing limits.
- Rough daylight112 is now reviewed on all three native backgrounds: refined preserves the small moving lower-left white streak, gray facets and perimeter highlights; draft weakens/restructures some subtle lower highlights, while detail remains grainy. rough-partial.json now holds12 reviewed-sheet hashes and24 comparisons. Analysis session8109 completed96 total comparisons with expected incomplete/missing-threshold status. Both daylight256 stationary references completed130185.740/128669.779ms; renderer session5583 is live on pose1 reference after all its candidates completed. Final rough selection/limits still require daylight256/512 review. No source or configuration changes.
- Rough final-source review completed: all18 native chronological sheets,36 comparisons and174 records, plus browser stepping of all four daylight112 refined poses including loop return. Selected refined (1024samples,one reconstruction pass). Max spatial2.315284,bias0.106681,temporal3.143636,seam3.185899,reference-noise0.939889 LSB; motion gain0.996437..0.998371. Reviewed case limits3.0/.15/3.8/3.8/1.1 are recorded in appearance-decisions/rough.json with all image/job/sheet hashes. Refined preserves gradients and narrow moving highlights better than draft and has substantially less grain than unfiltered detail; remaining512px grain and slight motion attenuation are explicit. No calibrated natural-wear claim. Analyzer28385 completed108 comparisons with expected incomplete/no-threshold status. Corpus5583 is live on localized_volume gameplay256 after completing its112 sequence; first256 reference completed53102.771ms. Four of seven cases reviewed; P3 and overall closure remain incomplete.
- Appearance review tool checkpoint3138db4 adds native causal-mask sheets beside the clear control, reference and candidates; no optical inputs or metric algorithms changed. Metric regression test passes, and112 earlier comparison records remain exactly unchanged. Evidence: feature-review-validation/report.json. Localized-volume gameplay review covers nine full sheets and five causal-mask sheets; local attenuation, internal gradients and their reflected/refracted copies remain visible. Partial evidence: appearance-decisions/localized_volume-partial.json. Final localized daylight review and selection are pending. Corpus5583 has completed localized volume and moved to resolved inclusions; overall acceptance remains incomplete.
- Localized-volume final-source review is complete: all18 native full sheets,ten causal-mask/control sheets,two rigs,three sizes/backgrounds,four poses,and browser daylight112 draft motion/loop return. Selected draft. Max spatial0.996180,bias0.072135,temporal1.316291,seam1.268133,reference-noise0.190528 LSB; motion gain0.998783..1.000717. Feature ROI255..6295 pixels,gain1.01654..1.04212,minimum correlation0.997197,maximum relative residual0.078409. Gradients,local attenuation and optical copies remain visible; the small feature-gain increase is explicit. Limits1.25/.1/1.6/1.6/.25 and126 record hashes are in appearance-decisions/localized_volume.json. Partial review superseded. Analyzer82101 completed132 comparisons with expected incomplete/missing-threshold status. Five of seven cases reviewed. Corpus5583 remains live on resolved-inclusion gameplay112; final acceptance remains pending.
- Final tool audit found and removed the obsolete no-op --keep-frames option in turn_gifs.gd (checkpoint b492007). Current two-gem/eight-frame headless planning passes, the retired flag fails before output creation, and both encoder regression tests pass. Evidence: turn-cli-final/report.json and logs. This tool is outside the frozen optical inventory. Resolved-inclusion gameplay112 review covers three full and three causal-mask sheets,four poses. Dark central crystal,boundary and reflected/refracted copies remain visible in both candidates; draft feature gain1.00019..1.00080,minimum correlation0.999448,max relative residual0.033256. Partial review saved; profile choice awaits larger sizes/daylight. Analyzer57012 completed136 comparisons with expected incomplete/missing-threshold status; corpus5583 is live on gameplay256 references.
- Selected-profile cost table now binds five reviewed decisions to15 measured output/profile rows in appearance-selected-costs.json/.md. It explicitly separates512px reprints from112/256px optical work, records median/p95/summed wall time, and retains concurrency/resume limitations. Inclusion/thin-boundary rows remain pending their reviews. Closure evidence index now has12 groups, adding current rotation CLI rejection/planning/encoder evidence and unchanged-metric causal-sheet validation.
- Draft requirement-by-requirement closure report saved outside active docs as closure-report-draft.md; it explicitly leaves P3 and dependent P5 closure open. Source-delta-audit.json verifies all113 frozen optical source hashes and accounts for all14 changed paths since ac58ff7 in six tool/presentation/documentation groups with exact evidence hashes. Core optics, authored catalog, scenes, autoloads and tests remain unchanged; worktree is clean at b492007. This is provenance evidence, not overall acceptance. Corpus5583 remains live on resolved-inclusion gameplay256 pose2 reference after completing prior stationary/pose1 references.
- Resolved-inclusion gameplay review now covers all nine native full sheets at112/256/512px,three backgrounds,four poses,and five causal-mask/control sheets. Browser stepping also covers all four112px draft poses including loop return,with no warnings/errors. Both profiles retain central dark crystal,narrow faceted boundary and reflected/refracted copies. Draft gameplay maxima: spatial0.978673,bias0.078412,temporal1.291271 LSB; reference noise0.178195. Partial evidence updated with12 comparisons and exact sheet hashes; daylight selection remains pending. Analyzer4819 completed144 comparisons with expected incomplete/missing-threshold status. Registry audit identifies109 Godot stages:106 were in the integrated run,latency is separately accepted,appearance is active,and optional crystal precision stress was not selected; normal crystal/independent reference checks passed.
- Resolved-inclusion daylight112 review adds three full and three causal-mask/control sheets. The lower-contrast rig preserves the central dark crystal,thin boundary,small lower highlight and optical copies across all four poses/backgrounds. Partial evidence now binds12 full sheets,eight feature sheets and16 comparisons. Analyzer44260 completed148 total comparisons with only expected incomplete/missing-threshold status. Corpus5583 is live on daylight256; first stationary reference completed150790.5ms. No optical source,configuration or profile limit changed.
- Resolved-inclusion final-source review is complete: all18 native full sheets,ten causal-mask/control sheets,two rigs,three sizes/backgrounds,four poses,and browser gameplay112 draft motion/loop return. Selected draft. Max spatial0.978673,bias0.078412,temporal1.291271,seam1.274465,reference-noise0.178195 LSB; motion gain0.998962..1.000236. Feature ROI273..7923 pixels,gain1.000002..1.001070,minimum correlation0.998739,maximum relative residual0.050331. Central crystal,narrow boundary,interior planes and optical copies remain visible; modest grain inside the dark inclusion at512px is explicit. Limits1.25/.1/1.6/1.6/.25 and126 record hashes are in appearance-decisions/resolved_inclusion.json. Partial review superseded. Six cases reviewed; selected cost table now covers18 rows. Corpus5583 remains live on thin_boundary gameplay256 after completing112; P3 and overall closure remain incomplete.
- Thin-boundary gameplay112 review covers three full and three causal-mask sheets plus browser stepping through all four draft/reference poses and loop return,with no warnings/errors. Causal ROI199..212 pixels retains gain1.00001..1.00102,minimum correlation0.999568,max relative residual0.029401. These small optical differences are mainly peripheral facet signals; this does not claim a spatially resolved6micrometer gap at112pixels. Partial evidence: appearance-decisions/thin_boundary-partial.json. Analyzer12866 completed160 comparisons with expected incomplete/missing-threshold status. Corpus5583 continues gameplay256 references; no final thin profile selected.
- Closure index now separately binds P0 replacement inspector execution/cache replay and current rotation CLI evidence to production clip planner/worker gates (13 inspected groups). Source parsing is explicitly not treated as utility execution. Corpus5583 remains live on thin-boundary gameplay256 after pose2 candidates; larger/daylight appearance review and strict qualification remain required. The prepared threshold publication helper is not run and no qualification metadata has been written.
- Thin-boundary gameplay review now covers all nine native full sheets at112/256/512px,three backgrounds,four poses,and five causal-mask/control sheets. Both candidates retain faint cavity footprint,narrow internal lines,peripheral highlights and optical copies. Draft has extra fine grain inside the512px cavity footprint; detail reduces it. Draft gameplay maxima: spatial1.064216,bias0.087772,temporal1.386710,seam1.366176,reference-noise0.220657 LSB; feature gain0.999917..1.001018,min correlation0.999163,max relative residual0.040948. Partial evidence updated with12 comparisons and exact reviewed hashes; daylight remains pending. Analyzer48233 completed168 comparisons with expected incomplete/no-threshold status. Corpus5583 is live on daylight112 pose2 reference. Prepared final docs audit helper remains unexecuted until archive/report closure.
- Thin-boundary daylight112 review adds three full and three causal-mask sheets. Small moving highlights,thin lines and internal contrast remain present; partial evidence now includes12 full sheets,eight feature sheets and16 comparisons. Previously reviewed sheet hashes are unchanged. Analyzer24018 completed172 comparisons with only expected incomplete/missing-threshold status. Corpus5583 continues daylight256 stationary references; first completed163452.46ms. AUTHORING_WORKFLOW now maps diagnostic profiles to actual request rung/samples/bounce/reconstruction/batch fields and explains internal/output dimensions and reprint-only512px costs. No optical sources or configuration changed.
- Final exported-field audit found one missed inert control: GemLightRig.dual_illuminant_kelvin is declared and saved in two rigs but has no active reader. P0 is reopened until its removal is validated. Other unique-name audit hits are intentional labels/metadata or active class-local calculations (banding period/phase,index range,print version); game-only fields are outside this engine change. Current corpus5583 finishes before any schema edit. Prepared one-time baseline capture compares every saved job's stored values (excluding only this field) and compiled lighting. Any result reuse must preserve original producer provenance, prove the exact declaration/data-only change, verify all930 request inputs and fresh render equivalence, and pass the ordinary current-source corpus validator. No production compatibility path or ignored control will remain. Current optical source/configuration are still unchanged.
- Corpus5583 completed all930 records and emitted CHECK_COMPLETE: appearance_corpus with exit0. Final native review covers all18 thin-boundary sheets andten causal-mask/control sheets plus earlier browser motion. Selected draft; maxima1.064216/.087772/1.386710/1.366176/.220657 LSB (spatial/bias/temporal/seam/reference noise),motion gain0.998775..1.000226,feature gain0.998615..1.001018,min correlation0.999163,max relative residual0.040948. All seven decisions and21 cost rows are recorded; reviewed thresholds.json published after verifying every decision/image/job/source hash. Strict baseline analysis is running in79719. Baseline schema capture55850 passed all930 admitted saved jobs and both compiled rigs,with positive marker. No rig source/data removal has run yet; prepared exact removal and current-plan equivalence scripts remain one-time ignored evidence tools. Overall/P0 closure remains open pending this cleanup,current-source qualification and final documentation.

- Final rig schema removal is implemented: one unused declaration/group and two saved values, with no executable method changes. Import,930-job active-input/compiled-light equivalence,nine affected CPU stages and three GPU stages pass. Fresh comparison produced17 identical display images;15 masters were byte-identical and two rounded raw views differed by at most4.76837158203125e-7. Direct and frozen job inputs are byte-identical; an initial binary-rounding hypothesis was rejected. Controlled1/16-sample dispatch grouping reproduces floating accumulation differences (max3.57627868652344e-6), consistent with the existing timing-adaptive dispatcher. The explicit reuse qualification preserves all original producers/payloads/costs and introduces no production exception. Current-schema cache verification and full default fresh asset build are in progress; strict current-source analysis/package closure remain pending.

- Final closure evidence: current-source strict appearance passes180comparisons with exactly unchanged metrics/costs; all930current worker cache reads pass with preserved original payloads/producers. Full default rebuild freshly rendered176masters and48reprints; all224jobs/304frames/48clips audited against original active inputs and current producer. Final package inventory and actual Play/validmerge/invalidswap/Menu/secondPlay pass. Native review covers all304rebuilt frames (295exact,nine<=1LSB). Isolated1080pprobe passes burstp95=15.352ms/restp95=15.608ms,64views,16bindings,no burst page loads,6,170,368resident texture bytes. Earlier ETW and error-screen evidence is carried only through explicit unchanged runtime source/executable proof. Current report consolidates requirements; this journal and approved plan are now archived with exact checksums.
